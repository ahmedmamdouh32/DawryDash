import { Component } from '@angular/core';
import { Button } from '../button/button';

@Component({
  selector: 'app-footer',
  imports: [Button],
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class Footer { }
